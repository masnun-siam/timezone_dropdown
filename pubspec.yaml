name: flutter_timezone_dropdown
description: "A new flutter widget for timezone dropdown"
version: 0.0.3
homepage: https://github.com/masnun-siam/timezone_dropdown

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:

  assets:
    - assets/json/timezone.json
